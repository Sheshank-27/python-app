name: Deploy Application

on:
  workflow_dispatch:

jobs:

  deploy:

    uses: Sheshank-27/reusable-workflows/.github/workflows/ecs-ci-cd.yml@main

    with:

      ecr_repository: python-app

      ecs_cluster: devops-cluster

      ecs_service: python-task-service

      task_definition: python-task

      container_name: python-container

      aws_region: ap-south-1

    secrets:

      AWS_ROLE_ARN: ${{ secrets.AWS_ROLE_ARN }}